# Example environment configuration for notes_backend
# ---------------------------------------------------
# 1. Copy this file to `.env` in the same directory.
# 2. Fill in the correct values for your environment.
# If the backend fails to start, ensure DATABASE_URL and other variables are set.

DATABASE_URL=postgresql+psycopg2://username:password@localhost:5432/note_db
JWT_SECRET_KEY=your_super_secret_jwt_key
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
